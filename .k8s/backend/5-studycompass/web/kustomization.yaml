apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/component: backend-5-studycompass-web

resources:
- deployment.yaml
- service.yaml

vars:
- name: BACKEND_5_STUDYCOMPASS_WEB_SERVICE
  objref:
    apiVersion: v1
    kind: Service
    name: web
